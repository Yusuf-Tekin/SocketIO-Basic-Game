import React, { useEffect } from 'react'

function User({ color, x, y,updateData }) {

    // setInterval(() => {
    //     let attr =  document.getElementById('myChar').getBoundingClientRect()
    //     updateData(attr.x,attr.y)
    // }, 1000);

    return (
        <div id = "myChar" >

        </div>
    )
}

export default User
